// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://xiaoshecode.github.io',
  base: '/paper4qi',
  trailingSlash: 'always',
  output: 'static',
});
